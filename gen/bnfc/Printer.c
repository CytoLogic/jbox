/* File generated by the BNF Converter (bnfc 2.9.6.1). */

/*** Pretty Printer and Abstract Syntax Viewer ***/

#include <ctype.h>   /* isspace */
#include <stddef.h>  /* size_t */
#include <stdio.h>
#include <string.h>
#include <stdlib.h>
#include "Printer.h"

#define INDENT_WIDTH 2

int _n_;
char *buf_;
size_t cur_;
size_t buf_size;

/* You may wish to change the renderC functions */
void renderC(Char c)
{
  if (c == '{')
  {
     onEmptyLine();
     bufAppendC(c);
     _n_ = _n_ + INDENT_WIDTH;
     bufAppendC('\n');
     indent();
  }
  else if (c == '(' || c == '[')
     bufAppendC(c);
  else if (c == ')' || c == ']')
  {
     removeTrailingWhitespace();
     bufAppendC(c);
     bufAppendC(' ');
  }
  else if (c == '}')
  {
     _n_ = _n_ - INDENT_WIDTH;
     onEmptyLine();
     bufAppendC(c);
     bufAppendC('\n');
     indent();
  }
  else if (c == ',')
  {
     removeTrailingWhitespace();
     bufAppendC(c);
     bufAppendC(' ');
  }
  else if (c == ';')
  {
     removeTrailingWhitespace();
     bufAppendC(c);
     bufAppendC('\n');
     indent();
  }
  else if (c == ' ') bufAppendC(c);
  else if (c == 0) return;
  else
  {
     bufAppendC(c);
     bufAppendC(' ');
  }
}

int allIsSpace(String s)
{
  char c;
  while ((c = *s++))
    if (! isspace(c)) return 0;
  return 1;
}

void renderS(String s)
{
  if (*s) /* s[0] != '\0', string s not empty */
  {
    if (allIsSpace(s)) {
      backup();
      bufAppendS(s);
    } else {
      bufAppendS(s);
      bufAppendC(' ');
    }
  }
}

void indent(void)
{
  int n = _n_;
  while (--n >= 0)
    bufAppendC(' ');
}

void backup(void)
{
  if (cur_ && buf_[cur_ - 1] == ' ')
    buf_[--cur_] = 0;
}

void removeTrailingSpaces()
{
  while (cur_ && buf_[cur_ - 1] == ' ') --cur_;
  buf_[cur_] = 0;
}

void removeTrailingWhitespace()
{
  while (cur_ && (buf_[cur_ - 1] == ' ' || buf_[cur_ - 1] == '\n')) --cur_;
  buf_[cur_] = 0;
}

void onEmptyLine()
{
  removeTrailingSpaces();
  if (cur_ && buf_[cur_ - 1 ] != '\n') bufAppendC('\n');
  indent();
}
char *printInput(Input p)
{
  _n_ = 0;
  bufReset();
  ppInput(p, 0);
  return buf_;
}
char *showInput(Input p)
{
  _n_ = 0;
  bufReset();
  shInput(p);
  return buf_;
}
void ppListJob(ListJob listjob, int i)
{
  if (listjob == 0)
  { /* nil */
  }
  else if (listjob->listjob_ == 0)
  { /* last */
    ppJob(listjob->job_, 0);
  }
  else
  { /* cons */
    ppJob(listjob->job_, 0);
    renderC(';');
    ppListJob(listjob->listjob_, 0);
  }
}

void ppInput(Input p, int _i_)
{
  switch(p->kind)
  {
  case is_StartInput:
    if (_i_ > 0) renderC(_L_PAREN);
    ppListJob(p->u.startInput_.listjob_, 0);
    if (_i_ > 0) renderC(_R_PAREN);
    break;

  default:
    fprintf(stderr, "Error: bad kind field when printing Input!\n");
    exit(1);
  }
}

void ppJob(Job p, int _i_)
{
  switch(p->kind)
  {
  case is_FGJob:
    if (_i_ > 0) renderC(_L_PAREN);
    ppCommandLine(p->u.fGJob_.commandline_, 0);
    if (_i_ > 0) renderC(_R_PAREN);
    break;

  case is_BGJob:
    if (_i_ > 0) renderC(_L_PAREN);
    ppCommandLine(p->u.bGJob_.commandline_, 0);
    renderC('&');
    if (_i_ > 0) renderC(_R_PAREN);
    break;

  default:
    fprintf(stderr, "Error: bad kind field when printing Job!\n");
    exit(1);
  }
}

void ppCommandLine(CommandLine p, int _i_)
{
  switch(p->kind)
  {
  case is_CmdLine:
    if (_i_ > 0) renderC(_L_PAREN);
    ppPipeline(p->u.cmdLine_.pipeline_, 0);
    ppOptRedir(p->u.cmdLine_.optredir_, 0);
    if (_i_ > 0) renderC(_R_PAREN);
    break;

  default:
    fprintf(stderr, "Error: bad kind field when printing CommandLine!\n");
    exit(1);
  }
}

void ppPipeline(Pipeline p, int _i_)
{
  switch(p->kind)
  {
  case is_SingleCmd:
    if (_i_ > 0) renderC(_L_PAREN);
    ppCommandPart(p->u.singleCmd_.commandpart_, 0);
    if (_i_ > 0) renderC(_R_PAREN);
    break;

  case is_PipeCmd:
    if (_i_ > 0) renderC(_L_PAREN);
    ppCommandPart(p->u.pipeCmd_.commandpart_, 0);
    renderC('|');
    ppPipeline(p->u.pipeCmd_.pipeline_, 0);
    if (_i_ > 0) renderC(_R_PAREN);
    break;

  default:
    fprintf(stderr, "Error: bad kind field when printing Pipeline!\n");
    exit(1);
  }
}

void ppListWord(ListWord listword, int i)
{
  if (listword == 0)
  { /* nil */
  }
  else if (listword->listword_ == 0)
  { /* last */
    ppIdent(listword->word_, 0); renderC(' ');
  }
  else
  { /* cons */
    ppIdent(listword->word_, 0);
    renderC(' ');
    ppListWord(listword->listword_, 0);
  }
}

void ppCommandPart(CommandPart p, int _i_)
{
  switch(p->kind)
  {
  case is_Cmd:
    if (_i_ > 0) renderC(_L_PAREN);
    ppListWord(p->u.cmd_.listword_, 0);
    if (_i_ > 0) renderC(_R_PAREN);
    break;

  default:
    fprintf(stderr, "Error: bad kind field when printing CommandPart!\n");
    exit(1);
  }
}

void ppOptRedir(OptRedir p, int _i_)
{
  switch(p->kind)
  {
  case is_NoRedir:
    if (_i_ > 0) renderC(_L_PAREN);
    if (_i_ > 0) renderC(_R_PAREN);
    break;

  case is_InOutRedir:
    if (_i_ > 0) renderC(_L_PAREN);
    renderC('<');
    ppIdent(p->u.inOutRedir_.word_1, 0);
    renderC('>');
    ppIdent(p->u.inOutRedir_.word_2, 0);
    if (_i_ > 0) renderC(_R_PAREN);
    break;

  case is_OutInRedir:
    if (_i_ > 0) renderC(_L_PAREN);
    renderC('>');
    ppIdent(p->u.outInRedir_.word_1, 0);
    renderC('<');
    ppIdent(p->u.outInRedir_.word_2, 0);
    if (_i_ > 0) renderC(_R_PAREN);
    break;

  case is_OutRedir:
    if (_i_ > 0) renderC(_L_PAREN);
    renderC('>');
    ppIdent(p->u.outRedir_.word_, 0);
    if (_i_ > 0) renderC(_R_PAREN);
    break;

  case is_InRedir:
    if (_i_ > 0) renderC(_L_PAREN);
    renderC('<');
    ppIdent(p->u.inRedir_.word_, 0);
    if (_i_ > 0) renderC(_R_PAREN);
    break;

  default:
    fprintf(stderr, "Error: bad kind field when printing OptRedir!\n");
    exit(1);
  }
}

void ppInteger(Integer n, int i)
{
  char tmp[20];
  sprintf(tmp, "%d", n);
  renderS(tmp);
}
void ppDouble(Double d, int i)
{
  char tmp[24];
  sprintf(tmp, "%.15g", d);
  renderS(tmp);
}
void ppChar(Char c, int i)
{
  bufAppendC('\'');
  bufEscapeC(c);
  bufAppendC('\'');
  bufAppendC(' ');
}
void ppString(String s, int i)
{
  bufAppendC('\"');
  bufEscapeS(s);
  bufAppendC('\"');
  bufAppendC(' ');
}
void ppIdent(String s, int i)
{
  renderS(s);
}

void ppWord(String s, int i)
{
  renderS(s);
}


void shListJob(ListJob listjob)
{
  bufAppendC('[');
  while(listjob != 0)
  {
    if (listjob->listjob_)
    {
      shJob(listjob->job_);
      bufAppendS(", ");
      listjob = listjob->listjob_;
    }
    else
    {
      shJob(listjob->job_);
      listjob = 0;
    }
  }
  bufAppendC(']');
}

void shInput(Input p)
{
  switch(p->kind)
  {
  case is_StartInput:
    bufAppendC('(');

    bufAppendS("StartInput");

    bufAppendC(' ');

    shListJob(p->u.startInput_.listjob_);

    bufAppendC(')');

    break;

  default:
    fprintf(stderr, "Error: bad kind field when showing Input!\n");
    exit(1);
  }
}

void shJob(Job p)
{
  switch(p->kind)
  {
  case is_FGJob:
    bufAppendC('(');

    bufAppendS("FGJob");

    bufAppendC(' ');

    shCommandLine(p->u.fGJob_.commandline_);

    bufAppendC(')');

    break;
  case is_BGJob:
    bufAppendC('(');

    bufAppendS("BGJob");

    bufAppendC(' ');

    shCommandLine(p->u.bGJob_.commandline_);

    bufAppendC(')');

    break;

  default:
    fprintf(stderr, "Error: bad kind field when showing Job!\n");
    exit(1);
  }
}

void shCommandLine(CommandLine p)
{
  switch(p->kind)
  {
  case is_CmdLine:
    bufAppendC('(');

    bufAppendS("CmdLine");

    bufAppendC(' ');

    shPipeline(p->u.cmdLine_.pipeline_);
  bufAppendC(' ');
    shOptRedir(p->u.cmdLine_.optredir_);

    bufAppendC(')');

    break;

  default:
    fprintf(stderr, "Error: bad kind field when showing CommandLine!\n");
    exit(1);
  }
}

void shPipeline(Pipeline p)
{
  switch(p->kind)
  {
  case is_SingleCmd:
    bufAppendC('(');

    bufAppendS("SingleCmd");

    bufAppendC(' ');

    shCommandPart(p->u.singleCmd_.commandpart_);

    bufAppendC(')');

    break;
  case is_PipeCmd:
    bufAppendC('(');

    bufAppendS("PipeCmd");

    bufAppendC(' ');

    shCommandPart(p->u.pipeCmd_.commandpart_);
  bufAppendC(' ');
    shPipeline(p->u.pipeCmd_.pipeline_);

    bufAppendC(')');

    break;

  default:
    fprintf(stderr, "Error: bad kind field when showing Pipeline!\n");
    exit(1);
  }
}

void shListWord(ListWord listword)
{
  bufAppendC('[');
  while(listword != 0)
  {
    if (listword->listword_)
    {
      shWord(listword->word_);
      bufAppendS(", ");
      listword = listword->listword_;
    }
    else
    {
      shWord(listword->word_);
      listword = 0;
    }
  }
  bufAppendC(']');
}

void shCommandPart(CommandPart p)
{
  switch(p->kind)
  {
  case is_Cmd:
    bufAppendC('(');

    bufAppendS("Cmd");

    bufAppendC(' ');

    shListWord(p->u.cmd_.listword_);

    bufAppendC(')');

    break;

  default:
    fprintf(stderr, "Error: bad kind field when showing CommandPart!\n");
    exit(1);
  }
}

void shOptRedir(OptRedir p)
{
  switch(p->kind)
  {
  case is_NoRedir:

    bufAppendS("NoRedir");




    break;
  case is_InOutRedir:
    bufAppendC('(');

    bufAppendS("InOutRedir");

    bufAppendC(' ');

    shIdent(p->u.inOutRedir_.word_1);
  bufAppendC(' ');
    shIdent(p->u.inOutRedir_.word_2);

    bufAppendC(')');

    break;
  case is_OutInRedir:
    bufAppendC('(');

    bufAppendS("OutInRedir");

    bufAppendC(' ');

    shIdent(p->u.outInRedir_.word_1);
  bufAppendC(' ');
    shIdent(p->u.outInRedir_.word_2);

    bufAppendC(')');

    break;
  case is_OutRedir:
    bufAppendC('(');

    bufAppendS("OutRedir");

    bufAppendC(' ');

    shIdent(p->u.outRedir_.word_);

    bufAppendC(')');

    break;
  case is_InRedir:
    bufAppendC('(');

    bufAppendS("InRedir");

    bufAppendC(' ');

    shIdent(p->u.inRedir_.word_);

    bufAppendC(')');

    break;

  default:
    fprintf(stderr, "Error: bad kind field when showing OptRedir!\n");
    exit(1);
  }
}

void shInteger(Integer i)
{
  char tmp[20];
  sprintf(tmp, "%d", i);
  bufAppendS(tmp);
}
void shDouble(Double d)
{
  char tmp[24];
  sprintf(tmp, "%.15g", d);
  bufAppendS(tmp);
}
void shChar(Char c)
{
  bufAppendC('\'');
  bufEscapeC(c);
  bufAppendC('\'');
}
void shString(String s)
{
  bufAppendC('\"');
  bufEscapeS(s);
  bufAppendC('\"');
}
void shIdent(String s)
{
  bufAppendC('\"');
  bufAppendS(s);
  bufAppendC('\"');
}

void shWord(String s)
{
  bufAppendC('\"');
  bufEscapeS(s);
  bufAppendC('\"');
}


void bufEscapeS(const char *s)
{
  if (s) while (*s) bufEscapeC(*s++);
}
void bufEscapeC(const char c)
{
  switch(c)
  {
    case '\f': bufAppendS("\\f" ); break;
    case '\n': bufAppendS("\\n" ); break;
    case '\r': bufAppendS("\\r" ); break;
    case '\t': bufAppendS("\\t" ); break;
    case '\v': bufAppendS("\\v" ); break;
    case '\\': bufAppendS("\\\\"); break;
    case '\'': bufAppendS("\\'" ); break;
    case '\"': bufAppendS("\\\""); break;
    default: bufAppendC(c);
  }
}

void bufAppendS(const char *s)
{
  size_t len = strlen(s);
  size_t n;
  while (cur_ + len >= buf_size)
  {
    buf_size *= 2; /* Double the buffer size */
    resizeBuffer();
  }
  for(n = 0; n < len; n++)
  {
    buf_[cur_ + n] = s[n];
  }
  cur_ += len;
  buf_[cur_] = 0;
}
void bufAppendC(const char c)
{
  if (cur_ + 1 >= buf_size)
  {
    buf_size *= 2; /* Double the buffer size */
    resizeBuffer();
  }
  buf_[cur_] = c;
  cur_++;
  buf_[cur_] = 0;
}
void bufReset(void)
{
  cur_ = 0;
  buf_size = BUFFER_INITIAL;
  resizeBuffer();
  memset(buf_, 0, buf_size);
}
void resizeBuffer(void)
{
  char *temp = (char *) malloc(buf_size);
  if (!temp)
  {
    fprintf(stderr, "Error: Out of memory while attempting to grow buffer!\n");
    exit(1);
  }
  if (buf_)
  {
    strncpy(temp, buf_, buf_size); /* peteg: strlcpy is safer, but not POSIX/ISO C. */
    free(buf_);
  }
  buf_ = temp;
}
char *buf_;
size_t cur_, buf_size;

